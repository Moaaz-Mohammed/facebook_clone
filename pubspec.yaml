name: facebook_clone
description: an asignment for exceptance of Maadi Company

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.1.0

  # Carousel Slider
  carousel_slider: ^4.0.0
  carousel_pro_nullsafety:

  #Responsive Builder to toggle between Mobile and Web
  responsive_builder: ^0.4.1

  # BloC State Management
  bloc:
  flutter_bloc: 7.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - images/
